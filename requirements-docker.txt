# 2026-04-05 | 新增 | Docker 专用依赖列表（CPU-only，排除 gsplat）
# 功能描述：VPS 无 GPU/CUDA，gsplat 需要 CUDA toolkit 编译 C++ 扩展，
#          而 SHARP predict 在非 CUDA 设备上会自动跳过 gsplat 渲染，
#          因此 Docker 部署可安全排除 gsplat。
# 设计思路：从 requirements.txt 提取所有非 gsplat / 非 CUDA / 非 triton 的依赖，
#          使用 CPU-only PyTorch 索引安装。

# ── CPU-only PyTorch（从官方 CPU 索引安装，体积约 200MB vs CUDA 版 2GB） ──
--extra-index-url https://download.pytorch.org/whl/cpu
torch
torchvision

# ── SHARP 核心依赖（与 pyproject.toml [project.dependencies] 一致，排除 gsplat） ──
click
imageio[ffmpeg]
matplotlib
pillow-heif
plyfile
scipy
timm

# ── FastAPI 后端依赖 ──
fastapi>=0.135
uvicorn>=0.43
python-multipart>=0.0.22
